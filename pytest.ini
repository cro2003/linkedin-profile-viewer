[pytest]
# bare `pytest` does not put the repo root on sys.path the way `python -m pytest`
# does, so `from app import ...` fails without this
pythonpath = .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
